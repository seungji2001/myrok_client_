export const ROUTES = {
  ROOT: '/',
  LOGIN: '/login',
  START: '/start',
  JOIN: '/join',
  CREATE: '/create',
  DASH_BOARD: '/dashboard',
  PROJECT_HOLDER: '/project-holder',
  RELOAD: 0,
} as const;
